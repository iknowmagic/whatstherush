"use client";

// Individual imports to avoid "export *" issue
import {
  motion,
  useScroll,
  useTransform,
  AnimatePresence,
  // Add any other components you need
} from "framer-motion";

export {
  motion,
  useScroll,
  useTransform,
  AnimatePresence,
  // Export the same components
};
